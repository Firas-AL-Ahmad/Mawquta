# Legacy Archive

This folder stores inactive placeholders and legacy stubs moved out of the active runtime path during the structure refactor.

Rules:
- Do not import modules from this folder in runtime code.
- Keep files only for historical reference or future cleanup.
- Promote a file back to `src/js` only when it becomes actively used.

